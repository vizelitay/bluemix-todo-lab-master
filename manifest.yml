applications:
- path: .
  memory: 512M
  instances: 1
  domain: mybluemix.net
  name: bluemix-todo
  host: bluemix-todo-${random-word}
declared-services:
  todo-db:
    label: cloudantNoSQLDB
    plan: Lite
  services:
  - todo-db
